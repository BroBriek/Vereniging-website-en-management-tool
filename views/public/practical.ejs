<%- include('../partials/header') %>

<div class="container mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Praktisch</h1>
        <% if (user) { %>
            <a href="/admin/page/practical" class="btn btn-chiro btn-sm">Bewerk Pagina</a>
        <% } %>
    </div>

    <div class="card p-4">
        <div class="card-body">
            <%- (content.main && content.main.content) ? content.main.content : '<p>Hier komt praktische info.</p>' %>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>