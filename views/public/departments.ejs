<%- include('../partials/header') %>

<div class="container mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Afdelingen</h1>
        <% if (user && user.role === 'admin') { %>
            <a href="/admin/page/departments" class="btn btn-chiro btn-sm">Bewerk Pagina</a>
        <% } %>
    </div>

    <div class="card p-4">
        <div class="card-body">
            <%- (content.main && content.main.content) ? content.main.content : '<p>Info over afdelingen.</p>' %>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>